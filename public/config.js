// Configuração de RUNTIME. Este arquivo é um placeholder vazio: em produção ele
// é reescrito quando o container sobe, pelo docker-entrypoint.d/40-app-config.sh,
// com os valores das variáveis de ambiente do container.
//
// Vazio aqui de propósito — em desenvolvimento a configuração vem do .env local
// (build-time), e nunca se versiona credencial no Git.
window.__APP_CONFIG__ = {};
